/**
 * Bundled Wowhead guide excerpts from scraped JSON (`knowledge/wowhead/scraped/`).
 * Client-safe: imported by buildContext. Keep payloads bounded.
 */

import talent_deathKnightBlood from '../../../knowledge/wowhead/scraped/death-knight-blood/talent-builds.json'
import rotation_deathKnightBlood from '../../../knowledge/wowhead/scraped/death-knight-blood/rotation-cooldowns.json'
import talent_deathKnightFrost from '../../../knowledge/wowhead/scraped/death-knight-frost/talent-builds.json'
import rotation_deathKnightFrost from '../../../knowledge/wowhead/scraped/death-knight-frost/rotation-cooldowns.json'
import talent_deathKnightUnholy from '../../../knowledge/wowhead/scraped/death-knight-unholy/talent-builds.json'
import rotation_deathKnightUnholy from '../../../knowledge/wowhead/scraped/death-knight-unholy/rotation-cooldowns.json'
import talent_demonHunterDevourer from '../../../knowledge/wowhead/scraped/demon-hunter-devourer/talent-builds.json'
import rotation_demonHunterDevourer from '../../../knowledge/wowhead/scraped/demon-hunter-devourer/rotation-cooldowns.json'
import talent_demonHunterHavoc from '../../../knowledge/wowhead/scraped/demon-hunter-havoc/talent-builds.json'
import rotation_demonHunterHavoc from '../../../knowledge/wowhead/scraped/demon-hunter-havoc/rotation-cooldowns.json'
import talent_demonHunterVengeance from '../../../knowledge/wowhead/scraped/demon-hunter-vengeance/talent-builds.json'
import rotation_demonHunterVengeance from '../../../knowledge/wowhead/scraped/demon-hunter-vengeance/rotation-cooldowns.json'
import talent_druidBalance from '../../../knowledge/wowhead/scraped/druid-balance/talent-builds.json'
import rotation_druidBalance from '../../../knowledge/wowhead/scraped/druid-balance/rotation-cooldowns.json'
import talent_druidFeral from '../../../knowledge/wowhead/scraped/druid-feral/talent-builds.json'
import rotation_druidFeral from '../../../knowledge/wowhead/scraped/druid-feral/rotation-cooldowns.json'
import talent_druidGuardian from '../../../knowledge/wowhead/scraped/druid-guardian/talent-builds.json'
import rotation_druidGuardian from '../../../knowledge/wowhead/scraped/druid-guardian/rotation-cooldowns.json'
import talent_druidRestoration from '../../../knowledge/wowhead/scraped/druid-restoration/talent-builds.json'
import rotation_druidRestoration from '../../../knowledge/wowhead/scraped/druid-restoration/rotation-cooldowns.json'
import talent_evokerAugmentation from '../../../knowledge/wowhead/scraped/evoker-augmentation/talent-builds.json'
import rotation_evokerAugmentation from '../../../knowledge/wowhead/scraped/evoker-augmentation/rotation-cooldowns.json'
import talent_evokerDevastation from '../../../knowledge/wowhead/scraped/evoker-devastation/talent-builds.json'
import rotation_evokerDevastation from '../../../knowledge/wowhead/scraped/evoker-devastation/rotation-cooldowns.json'
import talent_evokerPreservation from '../../../knowledge/wowhead/scraped/evoker-preservation/talent-builds.json'
import rotation_evokerPreservation from '../../../knowledge/wowhead/scraped/evoker-preservation/rotation-cooldowns.json'
import talent_hunterBeastMastery from '../../../knowledge/wowhead/scraped/hunter-beast-mastery/talent-builds.json'
import rotation_hunterBeastMastery from '../../../knowledge/wowhead/scraped/hunter-beast-mastery/rotation-cooldowns.json'
import talent_hunterMarksmanship from '../../../knowledge/wowhead/scraped/hunter-marksmanship/talent-builds.json'
import rotation_hunterMarksmanship from '../../../knowledge/wowhead/scraped/hunter-marksmanship/rotation-cooldowns.json'
import talent_hunterSurvival from '../../../knowledge/wowhead/scraped/hunter-survival/talent-builds.json'
import rotation_hunterSurvival from '../../../knowledge/wowhead/scraped/hunter-survival/rotation-cooldowns.json'
import talent_mageArcane from '../../../knowledge/wowhead/scraped/mage-arcane/talent-builds.json'
import rotation_mageArcane from '../../../knowledge/wowhead/scraped/mage-arcane/rotation-cooldowns.json'
import talent_mageFire from '../../../knowledge/wowhead/scraped/mage-fire/talent-builds.json'
import rotation_mageFire from '../../../knowledge/wowhead/scraped/mage-fire/rotation-cooldowns.json'
import talent_mageFrost from '../../../knowledge/wowhead/scraped/mage-frost/talent-builds.json'
import rotation_mageFrost from '../../../knowledge/wowhead/scraped/mage-frost/rotation-cooldowns.json'
import talent_monkBrewmaster from '../../../knowledge/wowhead/scraped/monk-brewmaster/talent-builds.json'
import rotation_monkBrewmaster from '../../../knowledge/wowhead/scraped/monk-brewmaster/rotation-cooldowns.json'
import talent_monkMistweaver from '../../../knowledge/wowhead/scraped/monk-mistweaver/talent-builds.json'
import rotation_monkMistweaver from '../../../knowledge/wowhead/scraped/monk-mistweaver/rotation-cooldowns.json'
import talent_monkWindwalker from '../../../knowledge/wowhead/scraped/monk-windwalker/talent-builds.json'
import rotation_monkWindwalker from '../../../knowledge/wowhead/scraped/monk-windwalker/rotation-cooldowns.json'
import talent_paladinHoly from '../../../knowledge/wowhead/scraped/paladin-holy/talent-builds.json'
import rotation_paladinHoly from '../../../knowledge/wowhead/scraped/paladin-holy/rotation-cooldowns.json'
import talent_paladinProtection from '../../../knowledge/wowhead/scraped/paladin-protection/talent-builds.json'
import rotation_paladinProtection from '../../../knowledge/wowhead/scraped/paladin-protection/rotation-cooldowns.json'
import talent_paladinRetribution from '../../../knowledge/wowhead/scraped/paladin-retribution/talent-builds.json'
import rotation_paladinRetribution from '../../../knowledge/wowhead/scraped/paladin-retribution/rotation-cooldowns.json'
import talent_priestDiscipline from '../../../knowledge/wowhead/scraped/priest-discipline/talent-builds.json'
import rotation_priestDiscipline from '../../../knowledge/wowhead/scraped/priest-discipline/rotation-cooldowns.json'
import talent_priestHoly from '../../../knowledge/wowhead/scraped/priest-holy/talent-builds.json'
import rotation_priestHoly from '../../../knowledge/wowhead/scraped/priest-holy/rotation-cooldowns.json'
import talent_priestShadow from '../../../knowledge/wowhead/scraped/priest-shadow/talent-builds.json'
import rotation_priestShadow from '../../../knowledge/wowhead/scraped/priest-shadow/rotation-cooldowns.json'
import talent_rogueAssassination from '../../../knowledge/wowhead/scraped/rogue-assassination/talent-builds.json'
import rotation_rogueAssassination from '../../../knowledge/wowhead/scraped/rogue-assassination/rotation-cooldowns.json'
import talent_rogueOutlaw from '../../../knowledge/wowhead/scraped/rogue-outlaw/talent-builds.json'
import rotation_rogueOutlaw from '../../../knowledge/wowhead/scraped/rogue-outlaw/rotation-cooldowns.json'
import talent_rogueSubtlety from '../../../knowledge/wowhead/scraped/rogue-subtlety/talent-builds.json'
import rotation_rogueSubtlety from '../../../knowledge/wowhead/scraped/rogue-subtlety/rotation-cooldowns.json'
import talent_shamanElemental from '../../../knowledge/wowhead/scraped/shaman-elemental/talent-builds.json'
import rotation_shamanElemental from '../../../knowledge/wowhead/scraped/shaman-elemental/rotation-cooldowns.json'
import talent_shamanEnhancement from '../../../knowledge/wowhead/scraped/shaman-enhancement/talent-builds.json'
import rotation_shamanEnhancement from '../../../knowledge/wowhead/scraped/shaman-enhancement/rotation-cooldowns.json'
import talent_shamanRestoration from '../../../knowledge/wowhead/scraped/shaman-restoration/talent-builds.json'
import rotation_shamanRestoration from '../../../knowledge/wowhead/scraped/shaman-restoration/rotation-cooldowns.json'
import talent_warlockAffliction from '../../../knowledge/wowhead/scraped/warlock-affliction/talent-builds.json'
import rotation_warlockAffliction from '../../../knowledge/wowhead/scraped/warlock-affliction/rotation-cooldowns.json'
import talent_warlockDemonology from '../../../knowledge/wowhead/scraped/warlock-demonology/talent-builds.json'
import rotation_warlockDemonology from '../../../knowledge/wowhead/scraped/warlock-demonology/rotation-cooldowns.json'
import talent_warlockDestruction from '../../../knowledge/wowhead/scraped/warlock-destruction/talent-builds.json'
import rotation_warlockDestruction from '../../../knowledge/wowhead/scraped/warlock-destruction/rotation-cooldowns.json'
import talent_warriorArms from '../../../knowledge/wowhead/scraped/warrior-arms/talent-builds.json'
import rotation_warriorArms from '../../../knowledge/wowhead/scraped/warrior-arms/rotation-cooldowns.json'
import talent_warriorFury from '../../../knowledge/wowhead/scraped/warrior-fury/talent-builds.json'
import rotation_warriorFury from '../../../knowledge/wowhead/scraped/warrior-fury/rotation-cooldowns.json'
import talent_warriorProtection from '../../../knowledge/wowhead/scraped/warrior-protection/talent-builds.json'
import rotation_warriorProtection from '../../../knowledge/wowhead/scraped/warrior-protection/rotation-cooldowns.json'

export type WowheadCoachingMode = 'compare' | 'solo'

const ROTATION_CHAR_BUDGET = 22_000

/**
 * All 40 retail (Midnight) specs, keyed by ChrSpecialization id. Adding/refreshing one:
 * `npm run scrape-wowhead -- <folder>` (registry in scripts/wowhead/scrape-wowhead.mjs),
 * then imports + an entry here. heroBranchNote is included in the compare
 * intro when the spec's Wowhead guide splits priorities by hero talent.
 */
const SPEC_DOCS: Record<number, { label: string; talent: unknown; rotation: unknown; heroBranchNote?: string }> = {
  62: { label: 'Arcane Mage', talent: talent_mageArcane, rotation: rotation_mageArcane },
  63: { label: 'Fire Mage', talent: talent_mageFire, rotation: rotation_mageFire },
  64: {
    label: 'Frost Mage',
    talent: talent_mageFrost,
    rotation: rotation_mageFrost,
    heroBranchNote: 'Wowhead uses hero-talent-specific priorities (Spellslinger vs Frostfire); ',
  },
  65: { label: 'Holy Paladin', talent: talent_paladinHoly, rotation: rotation_paladinHoly },
  66: { label: 'Protection Paladin', talent: talent_paladinProtection, rotation: rotation_paladinProtection },
  70: { label: 'Retribution Paladin', talent: talent_paladinRetribution, rotation: rotation_paladinRetribution },
  71: { label: 'Arms Warrior', talent: talent_warriorArms, rotation: rotation_warriorArms },
  72: { label: 'Fury Warrior', talent: talent_warriorFury, rotation: rotation_warriorFury },
  73: { label: 'Protection Warrior', talent: talent_warriorProtection, rotation: rotation_warriorProtection },
  102: { label: 'Balance Druid', talent: talent_druidBalance, rotation: rotation_druidBalance },
  103: { label: 'Feral Druid', talent: talent_druidFeral, rotation: rotation_druidFeral },
  104: { label: 'Guardian Druid', talent: talent_druidGuardian, rotation: rotation_druidGuardian },
  105: { label: 'Restoration Druid', talent: talent_druidRestoration, rotation: rotation_druidRestoration },
  250: { label: 'Blood Death Knight', talent: talent_deathKnightBlood, rotation: rotation_deathKnightBlood },
  251: { label: 'Frost Death Knight', talent: talent_deathKnightFrost, rotation: rotation_deathKnightFrost },
  252: { label: 'Unholy Death Knight', talent: talent_deathKnightUnholy, rotation: rotation_deathKnightUnholy },
  253: { label: 'Beast Mastery Hunter', talent: talent_hunterBeastMastery, rotation: rotation_hunterBeastMastery },
  254: { label: 'Marksmanship Hunter', talent: talent_hunterMarksmanship, rotation: rotation_hunterMarksmanship },
  255: { label: 'Survival Hunter', talent: talent_hunterSurvival, rotation: rotation_hunterSurvival },
  256: { label: 'Discipline Priest', talent: talent_priestDiscipline, rotation: rotation_priestDiscipline },
  257: { label: 'Holy Priest', talent: talent_priestHoly, rotation: rotation_priestHoly },
  258: { label: 'Shadow Priest', talent: talent_priestShadow, rotation: rotation_priestShadow },
  259: { label: 'Assassination Rogue', talent: talent_rogueAssassination, rotation: rotation_rogueAssassination },
  260: { label: 'Outlaw Rogue', talent: talent_rogueOutlaw, rotation: rotation_rogueOutlaw },
  261: { label: 'Subtlety Rogue', talent: talent_rogueSubtlety, rotation: rotation_rogueSubtlety },
  262: { label: 'Elemental Shaman', talent: talent_shamanElemental, rotation: rotation_shamanElemental },
  263: { label: 'Enhancement Shaman', talent: talent_shamanEnhancement, rotation: rotation_shamanEnhancement },
  264: { label: 'Restoration Shaman', talent: talent_shamanRestoration, rotation: rotation_shamanRestoration },
  265: { label: 'Affliction Warlock', talent: talent_warlockAffliction, rotation: rotation_warlockAffliction },
  266: { label: 'Demonology Warlock', talent: talent_warlockDemonology, rotation: rotation_warlockDemonology },
  267: { label: 'Destruction Warlock', talent: talent_warlockDestruction, rotation: rotation_warlockDestruction },
  268: { label: 'Brewmaster Monk', talent: talent_monkBrewmaster, rotation: rotation_monkBrewmaster },
  269: { label: 'Windwalker Monk', talent: talent_monkWindwalker, rotation: rotation_monkWindwalker },
  270: { label: 'Mistweaver Monk', talent: talent_monkMistweaver, rotation: rotation_monkMistweaver },
  577: { label: 'Havoc Demon Hunter', talent: talent_demonHunterHavoc, rotation: rotation_demonHunterHavoc },
  581: { label: 'Vengeance Demon Hunter', talent: talent_demonHunterVengeance, rotation: rotation_demonHunterVengeance },
  1480: { label: 'Devourer Demon Hunter', talent: talent_demonHunterDevourer, rotation: rotation_demonHunterDevourer },
  1467: { label: 'Devastation Evoker', talent: talent_evokerDevastation, rotation: rotation_evokerDevastation },
  1468: { label: 'Preservation Evoker', talent: talent_evokerPreservation, rotation: rotation_evokerPreservation },
  1473: { label: 'Augmentation Evoker', talent: talent_evokerAugmentation, rotation: rotation_evokerAugmentation },
}

export function wowheadReferenceAvailableForSpec(specId: number | undefined | null): boolean {
  if (specId == null || Number.isNaN(Number(specId))) return false
  return Number(specId) in SPEC_DOCS
}

function talentDoc(specId: number) {
  return SPEC_DOCS[specId]?.talent ?? null
}

function rotationDoc(specId: number) {
  return SPEC_DOCS[specId]?.rotation ?? null
}

function formatTalentSnapshot(specId: number): string {
  const t = talentDoc(specId) as {
    sourceUrl?: string
    talentCopies?: { label: string | null; importCode: string; heroTalent?: string | null }[]
    snapshot?: {
      wowheadDeclaredEdition?: { patchLabel?: { label?: string }; guideLastUpdated?: { displayDate?: string } }
      bbcodeSignals?: { seasonBanner?: { phrase?: string } }
    } | null
    blizzardTalentExports?: { samples?: unknown[] }
  } | null
  if (!t) return ''

  const rows = t.talentCopies ?? []
  const meta = t.snapshot
  const patch = meta?.wowheadDeclaredEdition?.patchLabel?.label ?? 'unknown'
  const updated = meta?.wowheadDeclaredEdition?.guideLastUpdated?.displayDate ?? 'unknown'
  const season = meta?.bbcodeSignals?.seasonBanner?.phrase ?? ''
  const header =
    `Wowhead **talent import table** (scraped): patch label ${patch}, guide updated ${updated}` +
    (season ? `, season tag ${season}` : '') +
    `. Source: ${t.sourceUrl ?? 'wowhead'}\n`

  if (rows.length > 0) {
    const lines = rows.map((r) => {
      const hero = r.heroTalent ? `${r.heroTalent} · ` : ''
      const codeShort = r.importCode.length > 48 ? `${r.importCode.slice(0, 48)}…` : r.importCode
      return `- ${hero}${r.label ?? 'build'}: \`${codeShort}\``
    })
    return header + lines.join('\n') + '\n'
  }

  const samples = (t.blizzardTalentExports?.samples as { heroTalent?: string; copy?: string }[] | undefined) ?? []
  if (samples.length > 0) {
    const lines = samples.slice(0, 12).map((s) => {
      const hero = s.heroTalent ? `${s.heroTalent} · ` : ''
      const c = s.copy ?? ''
      const codeShort = c.length > 48 ? `${c.slice(0, 48)}…` : c
      return `- ${hero}\`${codeShort}\``
    })
    return header + lines.join('\n') + '\n'
  }

  return header + '(no talent import rows in scrape)\n'
}

function formatRotationSnapshot(specId: number): string {
  const r = rotationDoc(specId) as {
    sourceUrl?: string
    sections?: { heading?: string | null; tocSlug?: string | null; body: string }[]
    snapshot?: {
      wowheadDeclaredEdition?: { patchLabel?: { label?: string }; guideLastUpdated?: { displayDate?: string } }
      bbcodeSignals?: { dbFlavor?: { tag?: string }; seasonBanner?: { phrase?: string } }
    }
  } | null
  if (!r) return ''

  const meta = r.snapshot
  const patch = meta?.wowheadDeclaredEdition?.patchLabel?.label ?? 'unknown'
  const updated = meta?.wowheadDeclaredEdition?.guideLastUpdated?.displayDate ?? 'unknown'
  let out =
    `Wowhead **rotation guide** (BBCode excerpts, scraped): patch label ${patch}, guide updated ${updated}. Source: ${r.sourceUrl ?? 'wowhead'}\n\n`

  const sections = r.sections ?? []
  let used = out.length
  out += '=== Sections (truncated per block) ===\n'
  for (const sec of sections) {
    const title = sec.tocSlug || sec.heading || '(intro)'
    const chunk = (sec.body ?? '').slice(0, 4500)
    const body = sec.body ?? ''
    const block = `\n--- ${title} ---\n${chunk}${body.length > 4500 ? '\n[…truncated…]' : ''}\n`
    if (used + block.length > ROTATION_CHAR_BUDGET) {
      out += '\n[Further sections omitted to stay within prompt budget — see source URL.]\n'
      break
    }
    out += block
    used += block.length
  }
  return out
}

function bodyCompareForSpec(specId: number): string {
  const doc = SPEC_DOCS[specId]
  if (!doc) return ''
  return (
    `Excerpts below are **scraped Wowhead BBCode** for ${doc.label} (Midnight-era guides), not live HTML. ` +
    `Use them together with the combat log and SimulationCraft block (if present). ` +
    `${doc.heroBranchNote ?? ''}infer which hero-talent branch matches **each** player's talents from the log before citing a priority list.\n` +
    `When Wowhead and SimC disagree, explain both and prefer **log evidence**.\n\n`
  )
}

function bodySoloForSpec(specId: number): string {
  const doc = SPEC_DOCS[specId]
  if (!doc) return ''
  return (
    `Excerpts below are **scraped Wowhead BBCode** for ${doc.label} — optional human-authored priorities alongside SimC. ` +
    `Treat them as **guide text**, not proof of optimal play for this pull.\n\n`
  )
}

/**
 * Reference text when `wowheadGroundedAnalysis` is true (bundled specs only).
 */
export function getWowheadReferenceSupplement(
  specId: number | undefined | null,
  grounded: boolean,
  _playerName: string | undefined,
  coachingMode: WowheadCoachingMode
): string {
  if (!grounded || specId == null || !(Number(specId) in SPEC_DOCS)) return ''

  const sid = Number(specId)
  const header =
    coachingMode === 'solo' ? `=== WOWHEAD — OPT-IN (SOLO) ===\n` : `=== WOWHEAD — OPT-IN COMPARISON ===\n`

  const bodyCompare = bodyCompareForSpec(sid)
  const bodySolo = bodySoloForSpec(sid)
  const talentBlock = formatTalentSnapshot(sid)
  const rotBlock = formatRotationSnapshot(sid)

  const footer = `<wowhead_reference>\n${talentBlock}\n${rotBlock}\n</wowhead_reference>\n\n`

  return header + (coachingMode === 'solo' ? bodySolo : bodyCompare) + footer
}
